# AnkerScale

A local BLE scale recorder and CLI for macOS, built primarily with MoonBit.

The project currently contains a native MoonBit scaffold. BLE collection,
SQLite storage, service management, and record-access commands are not implemented.

Run the scaffold:

```sh
moon run cmd/main
```
